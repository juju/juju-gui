{{#constraints}}
  <div class="control-group settings-wrapper">
   <div class="control-label" for="{{name}}">{{title}}</div>
    <div>
      <input class="constraint-field" type="text" name="{{name}}"
          value="{{value}}" data-bind="constraints.{{name}}" />
          {{unit}}
    </div>
    {{>conflict-ux}}
  </div>
{{/constraints}}
