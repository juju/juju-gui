{{#constraints}}
  <div class="settings-wrapper">
   <label for="{{name}}">{{title}}</label>
    <div>
      <input class="constraint-field" type="text" name="{{name}}"
          value="{{value}}" data-bind="constraints.{{name}}" />
          {{unit}}
    </div>
    {{>conflict-ux}}
  </div>
{{/constraints}}
