<ul id="unit-tiny" class="thumbnails">
  {{#units}}
  <li>
    <div class="well unit" id="{{id}}"
      title="{{number}}: {{agent_state}}{{#if public_address}} - {{public_address}}{{/if}}">
      <span class="agent-state">{{agent_state}}</span>
    </div>
  </li>
  {{/units}}
</ul>
