<ul id="unit-large" class="thumbnails">
  {{#units}}
  <li class="span2">
    <div class="well" id="{{id}}">
      <h5>{{number}}: {{agent_state}}</h5>
      <span>{{public_address}}</span>
    </div>
  </li>
  {{/units}}
</ul>
