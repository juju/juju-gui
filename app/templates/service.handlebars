{{> service-header}}
<div class="view-container">
<div>
  <div class="span12">
    <ul class="nav nav-tabs">
      {{#states}}
      <li {{#if active}}class="active"{{/if}}>
        <a href="{{link}}">{{title}}</a>
      </li>
      {{/states}}
    </ul>
  </div>
</div>

<div class="collection">
  <div class="charm">
    {{{show_units units}}}
  </div>
</div>

<div id="destroy-modal-panel"></div>
</div>

<div class="juju-service-info-container-bottom-menu">
	<div style="margin-top: 10px; margin-left: 10px;">
	  <div class="inline" style="width: 200px">
	   <a class="btn" id="destroy-service" data-toggle="modal" href="#destroy-service-modal">Destroy Service</a>
	  </div>
	  <div class="inline" style="width: 800x">
	   <a class="btn {{#if active}}{{/if}}" data-toggle="modal" href="#">All</a>
	   <a class="btn" data-toggle="modal" href="#">Running</a>
	   <a class="btn" data-toggle="modal" href="#">Pending</a>
	   <a class="btn" data-toggle="modal" href="#">Error</a>
	  </div>
	</div>
</div>  