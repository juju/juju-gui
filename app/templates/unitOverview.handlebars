<div class="unit-overview">
    <div class="header">
        <h1>{{ unit.displayName }}</h1>
        <ul class="unit-stats content">
            <li>IP Address: {{ unit.private_address }}</li>
            <li>Status: {{ unit.agent_state }}</li>
            {{#if unit.agent_state_info}}
              <li>Status Info: {{ unit.agent_state_info }}</li>
            {{/if}}
            <li>Public Address: {{ unitIPDescription }}</li>
            {{#if landscapeURL}}
              <li><a href="{{landscapeURL}}" class="landscape" target="_blank">Open Landscape</a></li>
            {{/if}}
        </ul>
    </div>
    <div class="unit-info-tabs content">
        <div class="unit-info-relations">
            <h3>Relations</h3>
            {{#if relations}}
            <table>
              <thead>
                <tr>
                  <th>id</th>
                  <th>endpoint</th>
                  <th>name</th>
                  <th>role</th>
                  <th>scope</th>
                  <th>status</th>
                </tr>
              </thead>
              <tbody>
                {{#relations}}
                  <tr>
                    <td class="relation-ident">{{ident}}</td>
                    <td class="relation-endpoint">
                      {{#if far}}
                      {{far.service}}
                      {{else}}
                      (self)
                      {{/if}}
                    </td>
                    <td class="relation-name">{{near.name}}</td>
                    <td class="relation-role">{{near.role}}</td>
                    <td class="relation-scope">{{scope}}</td>
                    <td class="relation-status">
                      {{#if has_error}}
                      <span class="error">&#x2b1b;</span> error
                      <div class="unit-button-palette">
                        <form id="{{near.name}}">
                        <button type="button" class="btn resolved-relation-button"
                          value="{{relation_id}}">Resolved</button>
                        <button type="button" class="btn retry-relation-button"
                          value="{{relation_id}}">Retry</button>
                        </form>
                      </div>
                      {{/if}}
                    </td>
                  </tr>
                {{/relations}}
              </tbody>
            </table>
            {{else}}
            Unit has no relations
            {{/if}}
        </div>
    </div>
</div>
