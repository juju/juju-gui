#!/bin/bash

`grep -lscor describe.only test`
if [ $? -ne 1 ]; then
    echo "describe.only left in test"
    exit 1
fi

`grep -lscor it.only test`
if [ $? -ne 1 ]; then
    echo "it.only left in test"
    exit 1
fi
